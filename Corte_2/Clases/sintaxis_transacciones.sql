BEGIN;
--SENTENCIAS SQL
ROLLBACK;
COMMIT;
--- Tienen un inicio
--- Dos posibles finales si tiene exito o no
---Autocommit = activo, cada vez que ejecutemos una sentencia dep
ende del cliente SET AUTOCOMMIT = 0
